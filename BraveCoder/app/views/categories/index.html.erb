<% title t('.title') %>
<% at_page t('.menu_link') %>

<% if current_user.admin? %>
  <%= link_to t('.add_category'), new_category_path, class: 'btn btn-primary btn-lg mt-2 mb-4' %>
<% end %>

<%= render @categories %>