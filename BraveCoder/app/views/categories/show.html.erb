<% title @category.title %>
<% at_page %>

<div class="btn-group">
  <%= link_to 'Add Event', new_category_event_path(@category), class: 'btn btn-primary btn-lg mt-2 mb-4' %>
  <%= link_to 'Edit category', edit_category_path(@category), class: 'btn btn-info btn-lg mt-2 mb-4' %>
</div>


<p class="lead"><%= @category.description %></p>

<h2>Events</h2>

<%= render @events %>